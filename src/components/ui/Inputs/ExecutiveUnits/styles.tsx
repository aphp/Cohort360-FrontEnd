import { styled } from '@mui/material'

export const ExecutiveUnitsWrapper = styled('div')(() => ({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
  border: '1px solid rgba(0,0,0,0.25)',
  padding: '5px 14px',
  width: '100%',
  borderRadius: '4px',
  color: 'rgba(0, 0, 0, 0.6)'
}))
