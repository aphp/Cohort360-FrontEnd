import { Box, styled } from '@mui/material'

export const BadgeWrapper = styled(Box)(() => ({
  backgroundColor: '#5bc5f2',
  color: '#FFF',
  borderRadius: 20,
  padding: '2px 6px',
  fontSize: 12,
  fontWeight: 'bold'
}))
