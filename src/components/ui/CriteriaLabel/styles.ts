import { FormLabel, styled } from '@mui/material'

export const StyledFormLabel = styled(FormLabel)(() => ({
  color: '#153d8a',
  fontWeight: 600,
  fontSize: 12,
  padding: '0 0 1em',
  display: 'flex',
  alignItems: 'center'
}))
