import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  card: {
    width: 450
  },
  cardHeader: {
    background: '#D1E2F4',
    color: '#45505B'
  }
}))

export default useStyles
