import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  tabs: {
    marginTop: 72,
    position: 'absolute',
    width: '100%'
  }
}))

export default useStyles
